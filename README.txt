This Repo is based on ML and DL , used NLP here.
Repo Contains the files which is need to run the program
The UI_of_model.py runs locally to produce the UI of the ML model

Before Running the these .py files install the necessary library packages mentioned in requiments.txt file

Has a PIE chart , a wordcloud(Max words used as a Wordcloud), a drop down box which contains reviews from the scrapped website and a text box area to 
check you own reviews there. 

Website from which the reviews taken : esty.com - jwellary section

etsay_scrapper.py is used for the data scrapping

.json file couldn't be uploaded as it above the upload limit of GitHub

Hope it helps for someone!
